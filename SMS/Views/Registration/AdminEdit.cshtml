@model SMS.Models.Registration

@{
    ViewData["Title"] = "Edit";
    Layout= "../Shared/_AdminPageLayout.cshtml";
}

<h1>Edit</h1>

<h4>Registration</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AdminEdit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="attendeeId" class="control-label"></label>
                <select asp-for="attendeeId" class="form-control" asp-items="ViewBag.attendeeId"></select>
                <span asp-validation-for="attendeeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="seminarId" class="control-label"></label>
                <select asp-for="seminarId" class="form-control" asp-items="ViewBag.seminarId"></select>
                <span asp-validation-for="seminarId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="AdminIndex">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
